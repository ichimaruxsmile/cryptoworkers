{
  "name": "cryptoscope-worker",
  "version": "1.0.0",
  "type": "module",
  "description": "CryptoScope edge API gateway — Cloudflare Worker serving mock crypto market data",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production"
  },
  "keywords": ["cloudflare", "workers", "crypto", "api-gateway"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
